rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For hackathon purposes, allow any authenticated user to read/write all data
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
// Force update
